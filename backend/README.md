# backend

## Purpose
This folder (`backend`) contains files for backend within the Home-Services-Marketplace project.

## Files
- `package-lock.json`: JSON configuration/data for package lock.
- `package.json`: JSON configuration/data for package.
- `server.js`: JavaScript module for server.

## Note
JSON and binary files are documented in this README because inline comments are unsafe for those formats.
